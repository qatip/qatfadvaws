AWS Lab 1

phase1-3_list-string_count
  Uses list(string) with count.
  Demonstrates initial deployment, safe append, and ordinal instability.

phase4_list-object_count
  Uses list(object) with count.
  Demonstrates that richer data structures do not fix ordinal identity problems.

phase5-6_map-object_foreach
  Uses map(object) with for_each.
  Demonstrates stable key-based identity and plan stability.

